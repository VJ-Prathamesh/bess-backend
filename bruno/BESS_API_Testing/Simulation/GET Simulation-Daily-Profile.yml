info:
  name: GET Simulation-Daily-Profile
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/projects/1/simulation/daily-profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
