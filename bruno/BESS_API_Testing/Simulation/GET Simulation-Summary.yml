info:
  name: GET Simulation-Summary
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/projects/1/simulation"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
