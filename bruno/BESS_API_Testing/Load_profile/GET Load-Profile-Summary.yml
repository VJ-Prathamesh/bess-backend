info:
  name: GET Load-Profile-Summary
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/projects/{id}/load-profile/summary"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
