info:
  name: PUT Load-Profile-update
  type: http
  seq: 2

http:
  method: PUT
  url: "{{baseUrl}}/projects/{id}/load-profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
