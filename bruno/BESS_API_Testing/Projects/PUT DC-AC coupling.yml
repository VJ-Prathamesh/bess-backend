info:
  name: PUT DC/AC coupling
  type: http
  seq: 3

http:
  method: PUT
  url: /projects/{id}/coupling-type
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
