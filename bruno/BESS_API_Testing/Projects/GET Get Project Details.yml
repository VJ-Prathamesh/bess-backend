info:
  name: GET Get Project Details
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}//projects/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
