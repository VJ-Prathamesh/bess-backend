info:
  name: POST Create Project
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/projects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
