info:
  name: pv_catlog
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/catalog/pv-modules"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
